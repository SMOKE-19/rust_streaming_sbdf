[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "streaming_sbdf_rs"
version = "0.1.0"
dependencies = ["duckdb==1.5.0"]

[tool.maturin]
python-source = "python"
